# Env & Secrets - HIDDEN (not pushed to GitHub)
.env
.env*
.env.local
.env.development
.env.development.local
.env.production
*.key
*.pem

# Developer mode - keep .env.development for reference, ignore real secrets
# But keep developer/.env.development as template (no real keys)
developer/.env.local

# Node
node_modules/
.next/
dist/
build/

# Python
__pycache__/
*.pyc
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# Developer configs - .env files are HIDDEN (not pushed to GitHub) but kept locally
# .env templates are hidden locally via hidden attribute, not tracked
# !developer/.env.development  # HIDDEN now
# !developer/.env.production   # HIDDEN now
!developer/developer.json
!developer/developer.prod.json

# Logs
*.log
